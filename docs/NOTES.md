These crash with lodash:

{ key: 'get' }
  - delete
{ key: 'isRegExp' }
  - support both string and regexp
{ key: 'isDate' }
  - support both string and date
{ key: 'trim' }
  - support both string and object
{ key: 'transform' }
  - rename to convert

Complete.